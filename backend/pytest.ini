[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    unit: Unit tests (service-level, no HTTP).
    integration: Integration tests (hit live API with httpx).
    integration_llm: Integration tests that exercise live LLM provider behavior (slower, opt-in lane).
